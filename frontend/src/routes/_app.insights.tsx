import { createFileRoute } from "@tanstack/react-router"; import { useQuery } from "@tanstack/react-query"; import { Card } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { PageHeader } from "@/components/ui/PageHeader"; import { platformApi } from "@/api/platform";
export const Route = createFileRoute("/_app/insights")({ component: InsightsPage });
function InsightsPage(){const q=useQuery({queryKey:["insights"],queryFn:platformApi.insights}); return <div className="space-y-6"><PageHeader eyebrow="AI Insights" title="Signals from uploaded data" description="Insights are generated from your current uploaded datasets."/><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{(q.data??[]).map((i:{title:string;description:string;priority:string;confidence:number;action:string})=><Card key={i.title} className="p-6"><Badge>{i.priority}</Badge><h3 className="mt-3 font-semibold">{i.title}</h3><p className="mt-2 text-sm text-muted-foreground">{i.description}</p><p className="mt-4 text-sm"><b>Recommended action:</b> {i.action}</p><p className="mt-3 text-xs text-muted-foreground">Confidence {(i.confidence*100).toFixed(0)}%</p></Card>)}</div></div>}
