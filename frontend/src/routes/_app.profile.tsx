import { createFileRoute } from "@tanstack/react-router"; import { useQuery } from "@tanstack/react-query"; import { Card } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Avatar, AvatarFallback } from "@/components/ui/avatar"; import { PageHeader } from "@/components/ui/PageHeader"; import { platformApi } from "@/api/platform";
export const Route=createFileRoute("/_app/profile")({component:ProfilePage}); function ProfilePage(){const q=useQuery({queryKey:["current-user"],queryFn:platformApi.me});const u=q.data;const name=u?.name??u?.email??"";return <div className="space-y-6"><PageHeader eyebrow="Profile" title="Your account" description="Authenticated account information."/><Card className="max-w-xl p-6"><div className="flex items-center gap-4"><Avatar className="h-16 w-16"><AvatarFallback>{name.split(" ").map((x:string)=>x[0]).join("")}</AvatarFallback></Avatar><div><h2 className="text-xl font-semibold">{name}</h2><p className="text-sm text-muted-foreground">{u?.email}</p><Badge className="mt-2">{u?.role}</Badge></div></div><dl className="mt-6 space-y-3 text-sm"><div className="flex justify-between"><dt className="text-muted-foreground">Account ID</dt><dd>{u?.id}</dd></div><div className="flex justify-between"><dt className="text-muted-foreground">Member since</dt><dd>{u?.created_at}</dd></div></dl></Card></div>}
