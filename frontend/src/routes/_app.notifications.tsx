import { createFileRoute } from "@tanstack/react-router"; import { useQuery } from "@tanstack/react-query"; import { Card } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { PageHeader } from "@/components/ui/PageHeader"; import { platformApi } from "@/api/platform";
export const Route=createFileRoute("/_app/notifications")({component:NotificationsPage}); function NotificationsPage(){const q=useQuery({queryKey:["notifications"],queryFn:platformApi.notifications});return <div className="space-y-6"><PageHeader eyebrow="Notifications" title="Your inbox" description="Live data quality and analytical signals."/><Card><ul className="divide-y">{(q.data??[]).map((n:{title:string;description:string;priority:string;created_at:string},i:number)=><li key={i} className="p-5"><div className="flex gap-2"><b>{n.title}</b><Badge variant="outline">{n.priority}</Badge></div><p className="text-sm text-muted-foreground">{n.description}</p><p className="mt-1 text-xs text-muted-foreground">{n.created_at}</p></li>)}</ul></Card></div>}
